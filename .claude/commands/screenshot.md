The MCP browser tools are configured in bridge.mcp.json. If tools are not available, load that config first.

Use the page_screenshot MCP tool to capture a screenshot of the current browser page. Show the screenshot to the user and describe what you see — layout, key elements, any errors or notable visual states.
