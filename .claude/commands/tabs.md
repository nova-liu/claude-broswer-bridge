The MCP browser tools are configured in bridge.mcp.json. If tools are not available, load that config first.

Use the list_tabs MCP tool to show all open browser tabs. Present them as a numbered list with title and URL. Ask the user which tab they want to switch to, or if they want to open a new tab or close one.
