The MCP browser tools are configured in bridge.mcp.json. If tools are not available, load that config first.

Use the cookies MCP tool to show all cookies for the current page. Present them in a readable table format with name, value, domain, and flags (httpOnly, secure). Highlight any authentication or session cookies.
