Use the page_snapshot MCP tool first to get the accessibility tree with element refs. Then use the drag tool to drag from the source element to the target element (specified by ref or description). Confirm the drag completed and show any resulting page change.
