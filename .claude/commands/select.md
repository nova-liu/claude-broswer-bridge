The MCP browser tools are configured in bridge.mcp.json. If tools are not available, load that config first.

Use the select_text MCP tool with the CSS selector the user provided to extract text content from matching elements. Report the extracted text.
