The MCP browser tools are configured in bridge.mcp.json. If tools are not available, load that config first.

Use the press_key MCP tool to press the keyboard key the user specified. Then use page_snapshot to show the result. Confirm what key was pressed and what happened.
